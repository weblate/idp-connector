# == Variables ==
#
# You can define here your project Variables.
# If you want to define/override a Variable for a specific Environment, 
# add an additional ‘variables.yaml’ file in a subdirectory in the ‘Config’ folder: 
# '<project>/Config/_<environment>/variables.yaml
#
Variables:
  idpConnectorDemo:
    # ID of the workflow that processes the submitted document. It can be either the name of a prebuilt workflow (e.g., invoice_extraction) or the ID of a custom workflow in UUID format.
    workflowId: 'invoice_extraction'
